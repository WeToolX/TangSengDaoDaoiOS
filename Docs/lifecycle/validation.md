# 验证记录

## 1. 当前状态

状态：WAIT_ACCEPT。

## 2. 已完成

- `client_req_id` 在编辑页实例内生成并复用，发布失败重试不会重新生成。
- 部分可见/不给谁看取消选择时恢复公开可见，并在上传前由客户端与服务端共同校验。
- 通知已读仅在请求成功后清零未读数；通知预览字段已补齐服务端响应与客户端解析。
- 评论回复、评论删除、动态删除、通知跳转动态和动态详情链路已接入现有 UI/接口。
- 图片上传已传递真实尺寸和字节数，使用实际 MIME/扩展名，并校验朋友圈文件路径。
- 动态 mentions 已解析；视频封面使用独立随机路径，固定封面 URL 增加版本参数避免旧缓存。
- 真实请求确认 `text` 在客户端提交前为空，已保留输入法提交修复。
- 针对真机请求仍出现 `text: ""`，发布前改为从 UITextView `textStorage`、输入系统范围和 attributedText 读取最终内容；Contacts 与主 App 模拟器构建再次通过。
- 增加 UITextView 实时 `draftText` 镜像，发送前记录文本长度但不记录正文，覆盖输入事件已发生而控件即时取值异常的情况。
- 视频播放图标已从文本字符替换为 SF Symbols `play.fill`，时间线和发布预览均已处理。

## 3. 待完成

- 真机发布重试、可见范围、通知和媒体上传验收。
- 收藏、位置搜索、陌生人可见范围修改和朋友圈状态修改没有可靠的 iOS 接口/UI 契约，尚未实现。
- 文字长度上限未实现，当前需求没有提供数值或服务端约束。
