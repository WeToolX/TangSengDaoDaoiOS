# 生命周期文档规范

## 1. 目录

- 需求文档位于 `requirements/`。
- 开发文档位于 `development/`。
- 验证记录位于 `validation.md`。

## 2. 状态

使用 `CONFIRMED`、`IN_PROGRESS`、`TEST_PASS`、`WAIT_ACCEPT`、`DONE` 表示状态。

## 3. 变更记录

每次变更记录原因、内容、影响范围和关联 ID。
